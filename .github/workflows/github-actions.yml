name: Flox Installer Smoke Test

run-name: ${{ github.actor }} is testing Flox install.sh

on: [push]

jobs:
  install:
    # skip install
    if: false
    name: Install on ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-24.04
            runs_on: ubuntu-24.04
            type: deb
          - name: ubuntu-22.04
            runs_on: ubuntu-22.04
            type: deb
          - name: ubuntu-24.04-arm
            runs_on: ubuntu-24.04-arm
            type: deb
          - name: ubuntu-22.04-arm
            runs_on: ubuntu-22.04-arm
            type: deb
          - name: macos-15-arm64
            runs_on: macos-15
            type: darwin
            arch: aarch64
          - name: macos-15-intel
            runs_on: macos-15-intel
            type: darwin
          - name: macos-14-arm64
            runs_on: macos-14
            type: darwin
            arch: aarch64
          - name: fedora-x86_64
            runs_on: ubuntu-latest
            type: rpm
          - name: fedora-arm
            runs_on: ubuntu-24.04-arm
            type: rpm
            
    runs-on: ${{ matrix.runs_on }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v5

      - name: Run install.sh (stable) on Debian/macOS
        if: matrix.type != 'rpm'
        run: |
          bash install.sh

      - name: Run install.sh (RPM path inside Fedora container)
        if: matrix.type == 'rpm'
        run: |
          docker run --rm -v "${{ github.workspace }}":/workspace -w /workspace registry.fedoraproject.org/fedora:latest \
            /bin/bash -eux -c "dnf -y install curl && bash install.sh && flox --version"

      - name: Verify flox is available (host)
        if: matrix.type != 'rpm'
        run: |
          command -v flox
          flox --version

  nix:
    name: Install on NixOS (${{ matrix.runs_on }})
    strategy:
      fail-fast: false
      matrix:
        runs_on: [ubuntu-latest, ubuntu-24.04-arm]
    runs-on: ${{ matrix.runs_on }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Run install.sh and verify flox
        run: |
          # Use sh as entrypoint to call bash from PATH
          docker run --rm -v "${{ github.workspace }}":/workspace -w /workspace \
            nixos/nix:latest \
            /bin/sh -c "bash ./install.sh"

  macos-nix:
    name: Install on macOS with Determinate Nix
    runs-on: macos-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v5
      - name: Install Determinate Nix
        uses: DeterminateSystems/nix-installer-action@v14
      - name: Run install.sh
        run: |
          bash install.sh
      - name: Verify flox
        run: |
          flox --version
